import XpNotificationManager from '../XpNotificationManager';

const RoutineComplete: React.FC<RoutineCompleteProps> = ({
  // ... existing props ...
}) => {
  // ... existing code ...

  return (
    <View style={styles.container}>
      <XpNotificationManager />
      {/* ... existing content ... */}
    </View>
  );
}; 